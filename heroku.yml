# build:
#   docker:
#     worker: Dockerfile 
# run:
#   worker: yarn start
build:
  docker:
    release:
       dockerfile: Dockerfile
       target: builder
    worker: Dockerfile
run:
  worker:
    command:
      - yarn start
    image: worker